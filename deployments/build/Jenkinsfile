pipeline {
  agent any
  stages {
    stage('prebuild') {
      agent {
        docker {
          image 'public.ecr.aws/y3f6w2p0/epuerta-challenge/webapp:latest'
        }

      }
      steps {
        echo 'prebuilding'
      }
    }

    stage('build') {
      steps {
        sh 'ls -la '
      }
    }

    stage('postbuild') {
      steps {
        echo 'postbuild'
      }
    }

  }
}